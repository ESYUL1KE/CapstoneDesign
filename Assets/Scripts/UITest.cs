using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UITest : MonoBehaviour
{
    public GameObject[] card;
    

    // Update is called once per frame
    void Update()
    {
        /*if (card[0] == card)
        {
            GameManager.Instance.PnText();
        }
        */
    }
}
